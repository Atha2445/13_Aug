#include<stdio.h>
#include<math.h>
int main()
{
    float radius,pi=3.14,a;
    float *r,*p,*a;
    radius=*r;
    pi=*p;
    a=*a;
    scanf("%f",r);
    *a=(*p)*(*r)**2;
    printf("Area of Circle=%f",*a);
    return 0;

}
